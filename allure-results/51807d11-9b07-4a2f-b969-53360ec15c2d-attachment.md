# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api/auth.login.spec.ts >> WC Sign In — API @api >> WC-005: incorrect password returns 422 and result false
- Location: e2e/tests/api/auth.login.spec.ts:21:9

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 422
Received: 403
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import { getApiBaseUrl, getDeviceType, getTestUser } from '../../utils/env';
  3  | import { loginMarketplaceWeb } from '../../utils/api-client';
  4  | import { expectJsonFailure } from '../../utils/validators';
  5  | 
  6  | /**
  7  |  * WC Sign In — API layer (parity with Postman "Log In" + AuthWebAPI_1_01_Test).
  8  |  */
  9  | test.describe('WC Sign In — API @api', () => {
  10 |     test('WC-001: valid credentials return access_token and user', async ({ request }) => {
  11 |         const cred = getTestUser();
  12 |         test.skip(!cred, 'Set TEST_USER_EMAIL and TEST_USER_PASSWORD in .env');
  13 | 
  14 |         const { token, body } = await loginMarketplaceWeb(request, cred!.email, cred!.password);
  15 | 
  16 |         expect(token.length).toBeGreaterThan(10);
  17 |         expect(body.user?.email?.toLowerCase()).toBe(cred!.email.toLowerCase());
  18 |         expect(body.user?.id).toBeGreaterThan(0);
  19 |     });
  20 | 
  21 |     test('WC-005: incorrect password returns 422 and result false', async ({ request }) => {
  22 |         const cred = getTestUser();
  23 |         test.skip(!cred, 'Set TEST_USER_EMAIL and TEST_USER_PASSWORD in .env');
  24 | 
  25 |         const res = await request.post(`${getApiBaseUrl()}/auth/login`, {
  26 |             headers: {
  27 |                 Accept: 'application/json',
  28 |                 'Content-Type': 'application/json',
  29 |                 'Device-Type': getDeviceType(),
  30 |             },
  31 |             data: {
  32 |                 email: cred!.email,
  33 |                 password: `${cred!.password}__wrong__`,
  34 |             },
  35 |         });
  36 | 
> 37 |         expect(res.status()).toBe(422);
     |                              ^ Error: expect(received).toBe(expected) // Object.is equality
  38 |         const body = (await res.json()) as Record<string, unknown>;
  39 |         expectJsonFailure(body);
  40 |         const errors = body.errors as Record<string, string>;
  41 |         expect(errors?.error).toContain('Incorrect');
  42 |     });
  43 | });
  44 | 
```